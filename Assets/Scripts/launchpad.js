#pragma strict

var ignition: boolean;
var platformHeight: float;
var platform: GameObject;

function Start () {
    platformHeight = 0.5;
}

function Update () {

}

function getPlatformHeight(){
    // var mesh: Mesh = platform.GetComponent.<MeshFilter>().mesh;
    // var bounds: Bounds = mesh.bounds;
    // var result = bounds.size.y;
    // return result;
}
